# Newsletter / mailing list.
# Set to "true" to render the Buttondown subscribe UI (hero form, header
# button, footer email link). Any other value (or unset) hides all of it, so
# the site ships with the mailing list disabled by default.
PUBLIC_NEWSLETTER_ENABLED=false
